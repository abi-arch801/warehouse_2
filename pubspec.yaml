name: warehouse
description: GudangPro — Sistem Manajemen Gudang Terintegrasi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Font & UI
  google_fonts: ^6.2.1

  # Format tanggal & angka
  intl: ^0.19.0

  # Lokasi GPS
  geolocator: ^13.0.2

  # Kamera / galeri
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true